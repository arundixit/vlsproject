<%= form_for(@payslipreport) do |f| %>
  <% if @payslipreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payslipreport.errors.count, "error") %> prohibited this payslipreport from being saved:</h2>

      <ul>
      <% @payslipreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
